---
description: Generate changelog from updates
---

Run `mc changelog` to generate a changelog from your update history.

Options:
- `--days N` - Include updates from last N days (default: 7)
- `--format md` - Output format: md, json (default: md)

This aggregates updates to show work history by date.
